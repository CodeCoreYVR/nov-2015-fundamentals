# We use gets to take input from the user in the terminal / console

# this will print `What is your name:` to the user
print "What is your name: "

# This will take string input from the user and stores it in `name` variable
name = gets

# This will print `Hello ` concatenated with the name entered by the user
puts "Hello " + name
